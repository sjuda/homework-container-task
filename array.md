## array

Реализуйте аналог контейнера std::array.

```cpp
template<class T, size_t N>
struct array
{
    array();
    ~array();
    
    begin
    end
    
    size
    empty
    
    operator[]
    at
    front
    back
    data
    
    fill
    swap
};
```
